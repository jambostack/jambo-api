ai:
    platform:
        openai:
            api_key: '%env(OPENAI_API_KEY)%'
        anthropic:
            api_key: '%env(ANTHROPIC_API_KEY)%'
        ollama:
            endpoint: '%env(OLLAMA_BASE_URL)%'
