# yaml-language-server: $schema=../vendor/symfony/dependency-injection/Loader/schema/services.schema.json

# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.
# See also https://symfony.com/doc/current/service_container/import.html

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones

    App\Service\WebhookSecretService:
        arguments:
            $webhookSecretKey: '%env(APP_WEBHOOK_SECRET_KEY)%'

    App\Repository\ApiTokenRepository:
        arguments:
            $appSecret: '%kernel.secret%'

    App\Controller\ProjectSettingsController:
        arguments:
            $appSecret: '%kernel.secret%'

    App\Service\ApiTokenChecker:
        arguments:
            $appSecret: '%kernel.secret%'

    App\Service\ExportImport\Export\MediaExportHandler:
        arguments:
            $projectDir: '%kernel.project_dir%'

    App\Service\ExportImport\Import\MediaImportHandler:
        arguments:
            $projectDir: '%kernel.project_dir%'

    App\Service\ExportImport\ProjectExporter:
        arguments:
            $projectDir: '%kernel.project_dir%'

    App\Service\ExportImport\ProjectImporter:
        arguments:
            $projectDir: '%kernel.project_dir%'

    App\Service\EndUserJwtService:
        arguments:
            $appSecret: '%kernel.secret%'

    App\Service\ImageTransformService:
        arguments:
            $projectDir: '%kernel.project_dir%'

    App\Controller\McpController:
        arguments:
            $mcpLimiter: '@limiter.mcp_limiter'

    App\Mcp\JamboApiMcpServer:
        lazy: true
        calls:
            - setAuditService: ['@App\Service\AuditService']

    App\Service\SearchService:
        arguments:
            $meilisearchHost: '%env(MEILISEARCH_HOST)%'
            $meilisearchKey: '%env(MEILISEARCH_KEY)%'

    # AiContentService — tous les fournisseurs IA sont configurés via l'admin
    # (Paramètres → Fournisseurs IA), les clés viennent de la base de données.

    # ── Email / SMTP ───────────────────────────────────────────────────────────
    App\Service\ProjectMailerService:
        arguments:
            $appSecret: '%kernel.secret%'

    # ── Storage / Multi-stockage ───────────────────────────────────────────────
    App\Service\StorageDriverFactory:
        arguments:
            $appSecret: '%kernel.secret%'
            $projectDir: '%kernel.project_dir%'

    App\Service\MediaSerializer:
        arguments:
            $profileRepo: '@App\Repository\ProjectStorageProfileRepository'
            $ruleRepo: '@App\Repository\StorageRuleRepository'
            $driverFactory: '@App\Service\StorageDriverFactory'
